@model IEnumerable<MaisonEcommerce.Models.Agendamento>

<p>
    <a asp-controller="Agenda" asp-action="CadastrarAgen" class="btn btn-primary">Cadastrar Agendamento</a>
</p>

@if (TempData["Mensagem"] != null)
{
    <div class="@TempData["Classe"]">@TempData["Mensagem"]</div>
}

<table class="table custom-table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.IdAgendamento)
            </th>

            <th>
                @Html.DisplayNameFor(model => model.NomeCliente)
            </th>

            <th>
                @Html.DisplayNameFor(model => model.NomeServico)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.DataHora)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.DataCadastro)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.DataAtualizacao)
            </th>

            <th class="text-center">Ação</th>
        </tr>
    </thead>

    <tbody>
        @foreach (var item in Model)
        {
            <tr>
                <td>
                    @Html.DisplayFor(modelItem => item.IdAgendamento)
                </td>

                <td>
                    @Html.DisplayFor(modelItem => item.NomeCliente)
                </td>

                <td>
                    @Html.DisplayFor(modelItem => item.NomeServico)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.DataHora)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.DataCadastro)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.DataAtualizacao)
                </td>
                <td class="text-center">
                    <a asp-controller="Agenda" asp-action="EditarAgenda" asp-route-id="@item.IdAgendamento" class="btn btn-sm btn-warning">Editar</a>
                    <span>|</span>
                    <a asp-controller="Agenda" asp-action="ExcluirAgenda" asp-route-id="@item.IdAgendamento"
                       onclick="return confirm('Deseja realmente Excluir este Agendamento?')" class="btn btn-sm btn-danger">Excluir</a>
                </td>
            </tr>
        }
    </tbody>
</table>